version: '3.11'

services:
  rl_algorithm:
    build:
      context: ./RL-UAM
    ports:
      - "6006:6006"       
    networks:
      - rl_network
    volumes:
      - ./RL-UAM:/app           


  vertisim_service:
    build:
      context: ./vertisim
    ports:
      - "5001:5001"
    networks:
      - rl_network
    volumes:
      - ./vertisim:/app      

  service_orchestrator:
    build:
      context: ./service-orchestrator
    ports:
      - "6000:6000"
    networks:
      - rl_network
    depends_on:
      - vertisim_service
    volumes:
      - ./service-orchestrator:/app   

networks:
  rl_network:
    driver: bridge